const nickname = "juandc";

function completeName(name, lastname){
    name = "Juan David";
    lastname = "Castro Gallego";
    complete = name + lastname;
    return complete;
};

console.log("Mi nombre es " + completeName(complete) + ", pero prefiero que me digas " + nickname + ".");